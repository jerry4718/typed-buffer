type RecyclableOption = {
    times?: number,
    judge?: (current: number) => boolean
}

function Recyclable(option: RecyclableOption, ) {

}

export function String() {
}

export function Array() {
}
